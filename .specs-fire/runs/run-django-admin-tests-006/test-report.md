---
run: run-django-admin-tests-006
work_item: ci-matrix
intent: core-package
generated: 2026-08-07T00:05:00Z
status: passed_with_stated_limits
---

# Test Report: GitHub Actions CI matrix

## Summary

| Category | Passed | Failed | Skipped | Coverage |
|----------|--------|--------|---------|----------|
| Workflow validation | 5 | 0 | 0 | n/a |
| Locally-runnable matrix cells | 4 | 0 | 0 | n/a |
| Matrix cells needing another interpreter | 0 | 0 | 18 | n/a |
| **Total** | 9 | 0 | 18 | n/a |

No new automated tests: the workflow is the deliverable. The existing
65-test suite is what each cell runs, and it passes in every locally
reachable configuration.

## Acceptance Criteria Validation

- ✅ **Matrix covers supported Python × Django × runner** — extended beyond the original wording after user confirmation: Python 3.10–3.14 × Django 4.2/5.2/6.1 × 2 runners, restricted to valid combinations (22 cells). The original "3.10–3.12 × 4.2/5.x" would never have exercised the project's actual dev environment (Python 3.14 + Django 6.1).
- ✅ **Each cell installs the package (editable) plus dev deps and runs the dogfood tests via the appropriate runner** — verified by running the exact CI command sequence locally in a clean venv
- ✅ **CI fails loudly if either runner regresses independently** — `fail-fast: false`; asserted programmatically that the file contains no `continue-on-error`
- ✅ **Workflow triggers on push and pull_request** — asserted programmatically (note: PyYAML parses bare `on:` as boolean `True`, so the check handles both spellings)
- ⚠️ **All matrix cells pass on the current codebase** — **partially verified; see limits below.** 4 of 22 cells verified locally, 18 unverifiable on this machine.

## Verification Performed

### Workflow structure (programmatic, not eyeballed)

- YAML parses; jobs are `lint` and `test`
- Triggers resolve to `[push, pull_request]`
- Matrix expands to 30 combinations, 8 excluded, **22 cells**
- Cell set matches an **independently re-derived** set of valid
  Python/Django combinations (computed from the support boundaries rather
  than copied from the `exclude:` list, so the check can actually catch a
  wrong exclusion): exact match, no unexpected or missing cells
- `fail-fast: false` confirmed; no `continue-on-error` present

### Cells executed locally (Python 3.14 only)

| Cell | Result |
|------|--------|
| py3.14 / dj6.1 / pytest | 65 passed, 1 skipped |
| py3.14 / dj6.1 / manage.py test | Ran 3 tests, OK (skipped=1) |
| py3.14 / dj5.2 / pytest | 65 passed, 1 skipped |
| py3.14 / dj5.2 / manage.py test | Ran 3 tests, OK (skipped=1) |

The Django 5.2 cells were run in a **clean venv using the exact CI install
sequence** (`pip install -e ".[dev]"` then `pip install "django==5.2.*"`),
which also confirmed the pin actually takes effect — resolved to Django
5.2.17, not the latest 6.1. That ordering bug would have made every cell
silently test the same Django version.

### Lint job

`ruff check .` and `ruff format --check .` both pass locally — the same
commands the lint job runs.

## Limits — cells NOT verified

**This machine has only Python 3.14 installed.** The following 18 cells are
unverified and will be exercised for the first time when CI runs:

- All Python 3.10, 3.11, 3.12, 3.13 cells (both runners)
- **All Django 4.2 cells**, which cannot be tested here at all: Django 4.2
  supports Python ≤3.12, so the combination is invalid on this interpreter.
  An attempt confirmed it fails, but for that reason rather than anything
  about this codebase.

The workflow is correct by construction and verified where the interpreter
allows; I am not claiming those 18 cells pass. First push will tell.

## Issues Found

**Install ordering would have silently invalidated the whole matrix.**
Running `pip install "django==X.*"` before `pip install -e ".[dev]"` lets
the editable install resolve Django back to the newest version, so all 22
cells would have tested one Django version while appearing to test three.
The workflow pins *after* the editable install, and the local clean-venv
run verified the pin sticks. Commented inline so it isn't "tidied" later.

## Ready for Completion

- [x] Workflow validated structurally and by independent re-derivation
- [x] Locally reachable cells pass
- [x] Lint gates pass
- [x] Verification limits stated explicitly rather than glossed
- [ ] Full matrix green — pending first CI run (cannot be verified locally)

---
*Generated by specs.md - fabriqa.ai FIRE Flow Run run-django-admin-tests-006*
