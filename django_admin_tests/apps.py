from django.apps import AppConfig


class DjangoAdminTestsConfig(AppConfig):
    """AppConfig for django_admin_tests, registered via INSTALLED_APPS."""

    default_auto_field = "django.db.models.BigAutoField"
    name = "django_admin_tests"
